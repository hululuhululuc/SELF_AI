# SELF_AI
Self AI is a model-centered coding agent framework. The core philosophy is simple: let the model own decision-making, while the system enforces boundaries, permissions, tool execution, and observable failure semantics.
